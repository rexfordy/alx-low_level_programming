#include <stdio.h>
/**
* main - prints all single digit numbers of base
* 10 starting from 0 to 9, followed by a new line
* Return: 0 (Success)
*/
int main(void)
{
int s;
for (s = 0; s < 10; s++)
printf("%i", s);
printf("\n");
return (0);
}
